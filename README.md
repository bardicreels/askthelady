VTT update procedure

Add new files to vtt/ 
Make sure they follow the correct naming convention:

video_title.Youtube_Video_Code.vtt

Run in a python Virtual environment (Cmd: pystart)
```
$ python update.py
$ git add .
$ git commit -m "update"
$ git push
```
